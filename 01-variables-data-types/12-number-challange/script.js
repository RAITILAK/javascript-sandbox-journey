const x = Math.floor(Math.random() * 100 + 1);
const y = Math.floor(Math.random() * 50 + 1);

const sum = x + y;
const sumOutput = `The sum of ${x} and ${y} is ${sum}`;
const difference = x - y;
const product = x * y;
const quotient = x / y;
const remainder = x % y;
console.log(sumOutput);
